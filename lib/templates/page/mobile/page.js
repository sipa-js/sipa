class {{class}} extends SipaBasicView {
    static onInit() {
        // called when page has been loaded, before fade animation
    }

    static onDestroy() {
        // called when leaving page, before next page will be loaded
    }

    static onHide() {
        // called when hiding page, when other page is stacked on top or page is closed
    }

    static onShow() {
        // called when showing page, when page is loaded first time, or stacked page is closed
    }
}