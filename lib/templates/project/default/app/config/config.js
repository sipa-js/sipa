
sipa.setConfig({
   defaults: {
      layout: 'default',
      page: 'home-page',
      // overwrite default layouts for specific pages
      layouts: {
         // EXAMPLE:
         // 'settings-page': 'mini-layout'
      }
   }
});