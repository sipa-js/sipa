SipaOnsenHooks.beforeInitPage('on', () => {
    // put some custom stuff here that is execute on
    // every page before it will be initialized
});

SipaOnsenHooks.beforeShowPage('on', () => {
    // put some custom stuff here that is execute on
    // every page before it will be shown
});

SipaOnsenHooks.beforeHidePage('on', () => {
    // put some custom stuff here that is execute on
    // every page before it will be hidden
});

SipaOnsenHooks.beforeDestroyPage('on', () => {
    // put some custom stuff here that is execute on
    // every page before it will be destroyed
});

SipaOnsenHooks.beforeInitLayout('on', () => {
    // put some custom stuff here that is execute on
    // every layout before it will be initialized
});

SipaOnsenHooks.beforeDestroyLayout('on', () => {
    // put some custom stuff here that is execute on
    // every layout before it will be destroyed
});